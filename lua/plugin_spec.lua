---@class Plugin
---@field [1]? string "Short plugin url."
---@field dir? 	string "A directory pointing to a local plugin."
---@field url? 	string "A custom git url where the plugin is hosted."
---@field name? string "A custom name for the plugin."
---@field dev? 	boolean "When `true`, a local plugin directory will be used instead."
---@field lazy? boolean "When `true`, the plugin will be only loaded when needed."
---@field enabled? boolean | fun(): boolean "When false, or if the function returns false, then this plugin will not be included in the spec"
---@field cond? boolean | fun(plugin: Plugin): boolean "When false, or if the function returns false, then this plugin will not be loaded. Useful to disable some plugins in vscode, or firenvim for example."
---@field dependencies? Plugin[] | string[] "A list of plugin names or plugin specs that should be loaded when the plugin loads. Dependencies are always lazy-loaded unless specified otherwise. When specifying a name, make sure the plugin spec has been defined somewhere else."
---@field init? fun(plugin: Plugin) "`init` functions are always executed during startup"
---@field opts? table | fun(plugin: Plugin, opts: table): table "`opts` should be a table (will be merged with parent specs), return a table (replaces parent specs) or should change a table. The table will be passed to the `Plugin.config()` function. Setting this value will imply `Plugin.config()`"
---@field config? fun(plugin: Plugin, opts: table) | boolean "`config` is executed when the plugin loads. The default implementation will automatically run `require(MAIN).setup(opts)`. Lazy uses several heuristics to determine the plugin's MAIN module automatically based on the plugin's name. See also `opts`. To use the default implementation without `opts` set `config` to `true`."
---@field main? string "You can specify the main module to use for `config()` and `opts()`, in case it can not be determined automatically. See `config()`"
---@field build? string | fun(Plugin) "`build` is executed when a plugin is installed or updated. Before running `build`, a plugin is first loaded. If it's a string it will be ran as a shell command. When prefixed with `:` it is a Neovim command. You can also specify a list to executed multiple build commands. Some plugins provide their own `build.lua` which is automatically used by lazy. So no need to specify a build step for those plugins."
---@field branch? string "Branch of the repository."
---@field tag? string "Tag of the repository."
---@field commit? string "Commit of the repository."
---@field version? string "Version to use from the repository. Full Semver ranges are supported."
---@field pin? boolean "When `true`, this plugin will not be included in updates."
---@field submodules? boolean "When `false`, git submodules will not be fetched. Defaults to `true`"
---@field event? string | string[] | fun(self:Plugin, event:string[]): string[] "Lazy-load on `event`. Events can be specified as `BufEnter` or with a pattern like `BufEnter *.lua`."
---@field cmd? string | string[] | fun(self:Plugin, event:string[]): string[] "Lazy-load on command."
---@field ft? string | string[] | fun(self:Plugin, event:string[]): string[] "Lazy-load on filetype."
---@field keys? string | string[] | Key[] | fun(self:Plugin, keys:string[]): (string | Key)[] "Lazy-load on key mapping."
---@field module? boolean "Do not automatically load this Lua module when it's required somewhere"
---@field priority? number "Only useful for start plugins (`lazy=false`) to force loading certain plugins first. Default priority is `50`. It's recommended to set this to a high number for colorschemes."
---@field optional? boolean "When a spec is tagged optional, it will only be included in the final spec, when the same plugin has been specified at least once somewhere else without `optional`. This is mainly useful for Neovim distros, to allow setting options on plugins that may/may not be part of the user's plugins"

---@class Key
---@field [1] string "lhs"
---@field [2]? string | fun() "rhs"
---@field mode? string | string[] "mode, defaults to `n`."
---@vararg any "any other option of `vim.keymap.set`"
